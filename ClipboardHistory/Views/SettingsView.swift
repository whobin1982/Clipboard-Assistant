import SwiftUI

struct SettingsView: View {
    var body: some View {
        Form {
            Text("Clipboard History Settings")
        }
        .padding()
        .frame(width: 420, height: 260)
    }
}
